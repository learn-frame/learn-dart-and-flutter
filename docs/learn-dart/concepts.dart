/// 所有变量引用的都是对象, 每个对象都是一个类的实例. 数字, 函数以及 null 都是对象
/// 所有的类都继承于 Object 类
///
/// Dart 是强类型语言, 用 var 或者 const 修饰的变量一旦声明就不可修改
/// 尽管如此, 你仍然可以用 dynamic 来修饰一个类型不确定的变量
///
/// Dart 支持泛型系统, 比如 List<int>
///
/// Dart 支持顶级函数和顶级变量
///
/// Dart 没有 public, protected, private 等修饰符, 它通过下划线来表示库内的私有成员
///
/// 标识符可以以字母或者下划线开头, 其后可跟字符和数字的组合
///
/// Dart 中 表达式 和 语句 是有区别的, 表达式有值而语句没有
